node_modules
artifacts
cache
.env
server/subscribers.json
coverage
coverage.json
dist
.audit-venv
.mythril-venv

# Rust/Anchor (programs/subscription-manager) — target/ is Cargo's build
# output, regenerated by `anchor build`. The deploy keypair's private key
# controls program upgrades, same sensitivity as a deployer private key
# (.env above) — never committed.
target
.anchor

